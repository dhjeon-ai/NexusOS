version: 1
display_name: NexusOS Bootstrap
short_description: Bootstraps a durable agent work standard with repository contracts and low-context docs.
default_prompt: Set up NexusOS for this repository so agents share the same paths, context-loading rules, task flow, and verification defaults.
